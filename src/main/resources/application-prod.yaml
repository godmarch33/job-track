spring:
  r2dbc:
    url: r2dbc:postgresql://postgres:5432/job-track
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/job-track
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  docker:
    compose:
      enabled: false

open:
  api:
    key: ${OPEN_API_KEY}
