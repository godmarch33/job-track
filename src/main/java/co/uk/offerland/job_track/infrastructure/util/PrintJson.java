package co.uk.offerland.job_track.infrastructure.util;import org.springframework.stereotype.Component;import com.fasterxml.jackson.databind.ObjectMapper;import lombok.RequiredArgsConstructor;import lombok.SneakyThrows;@Component@RequiredArgsConstructorpublic class PrintJson {    private static final String RESET = "\u001B[0m";    private static final String YELLOW = "\u001B[33m";  // Yellow for keys    private static final String BLUE = "\u001B[34m";  // Blue for values    private static final String RED = "\u001B[31m";    private final ObjectMapper objectMapper;    @SneakyThrows    public String json(Object json) {        return objectMapper.writeValueAsString(json).replaceAll("(\"[^\"]+\")\\s*:", YELLOW + "$1" + RESET + ":")  // Keys in yellow (like Postman)            .replaceAll(": (\"[^\"]+\")", ": " + BLUE + "$1" + RESET)  // String values in blue (like Postman)            .replaceAll(": (\\d+)", ": " + BLUE + "$1" + RESET)  // Numeric values in blue            .replaceAll(": null", ": " + RED + "null" + RESET);  // Null values in red    }}